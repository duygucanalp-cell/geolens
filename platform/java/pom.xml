<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>dev.geolens</groupId>
  <artifactId>geolens</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>geolens</name>
  <description>GeoLens modüler monolit spike (Go port evaluation) — JDK 25 + Spring Boot 3 + virtual threads. Tek ikili; recommendation ve sentiment bağlam paketleri.</description>

  <properties>
    <java.version>25</java.version>
    <!-- Varsayılan çalıştırmada integration grubu dışlanır.
         Docker ile entegrasyon: mvnw test -Dsurefire.groups=integration -->
    <surefire.groups>!integration</surefire.groups>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <!-- ADR-014: veri erişim katmanı JOOQ (type-safe SQL, RLS dostu).
         JdbcTemplate hâlâ kontrolör/service katmanında kullanılıyor; DAO'lar DSLContext üzerinden çalışır. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jooq</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- auth: bcrypt şifre hash'leme (Go golang.org/x/crypto/bcrypt karşılığı) -->
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-crypto</artifactId>
    </dependency>

    <!-- pdf: PDF üretimi (Go github.com/johnfercher/maroto/v2 karşılığı) -->
    <dependency>
      <groupId>com.github.librepdf</groupId>
      <artifactId>openpdf</artifactId>
      <version>1.3.30</version>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>25</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <groups>${surefire.groups}</groups>
        </configuration>
      </plugin>
      <!-- ADR-014: JOOQ codegen — DDLDatabase, canlı DB olmadan migration SQL'lerinden üretir.
           Yalnızca DAO'ların kullandığı tabloları kapsayan migration alt kümesi (sıralı). -->
      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>
        <version>3.19.35</version>
        <dependencies>
          <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq-meta-extensions</artifactId>
            <version>3.19.35</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>jooq-generate</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <generator>
            <name>org.jooq.codegen.JavaGenerator</name>
            <database>
              <name>org.jooq.meta.extensions.ddl.DDLDatabase</name>
              <properties>
                <!-- Küratörlü şema (src/main/resources/ddl/geolens.sql) — üretim
                     migration'larından kopya, RLS/function/extension ifadeleri hariç.
                     OSS jOOQ parser'ı CREATE FUNCTION'ı çözemediği için birebir
                     migration dosyaları kullanılamaz. -->
                <property>
                  <key>scripts</key>
                  <value>src/main/resources/ddl/geolens.sql</value>
                </property>
                <!-- PostgreSQL: tırnaksız tanımlayıcılar küçük harfe iner (H2 varsayılanı büyük). -->
                <property>
                  <key>defaultNameCase</key>
                  <value>lower</value>
                </property>
              </properties>
            </database>
            <generate>
              <javaTimeTypes>true</javaTimeTypes>
              <records>true</records>
              <pojos>false</pojos>
              <daos>false</daos>
            </generate>
            <target>
              <packageName>dev.geolens.jooq</packageName>
            </target>
          </generator>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>